<div class="container">
  <div class="mt-3 mx-auto" style="max-width: 350px">
    <%= render @item %>
    
    <div>
      <%= link_to "Edit this item", edit_item_path(@item) %> |
      <%= link_to "Back to items", items_path %>
    
      <%= button_to "Destroy this item", @item, method: :delete, class: "mt-2 btn btn-sm btn-block btn-primary" %>
    </div>
  </div>
</div>
